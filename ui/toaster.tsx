'use client';

/**
 * Phase 1 placeholder. In Phase 2 we'll wire this to a real toast provider
 * (likely Radix Toast or Sonner). For now it's a no-op so the layout compiles.
 */
export function Toaster() {
  return null;
}
